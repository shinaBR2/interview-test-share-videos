import { render, screen } from "@testing-library/react";

describe("Share Movie Dialog", () => {
  it("should render dialog correctly", () => {});
  it("should show error when no url entered", () => {});
  it("should show success message when enter url", () => {});
  it("should close share video dialog upon success", () => {});
});
